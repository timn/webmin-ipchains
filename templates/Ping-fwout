##-> Allows the firewall to ping hosts on the outside network. These rules 
##-> maybe useful on a personal firewall.
@IPCHAINS@ -A output -i @EXTDEV@ -s @EXTIP@ -d @EXTNET@ -p icmp --icmp-type echo-request -j ACCEPT
@IPCHAINS@ -A input -i @EXTDEV@ -s @EXTNET@ -d @EXTIP@ -p icmp --icmp-type echo-reply -j ACCEPT
