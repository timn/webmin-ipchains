##-> Allows clients on the internal network to access a NFS server running
##-> on the firewall host. NFS is very unsecure and is NOT recommended to run 
##-> this service on the firewall machine!
@IPCHAINS@ -A input -i @INTDEV@ -s @INTNET@ 1024:65535 -d @INTIP@ 111 -p udp -j ACCEPT
@IPCHAINS@ -A output -i @INTDEV@ -s @INTIP@ 111 -d @INTNET@ 1024:65535 ! -y -p udp -j ACCEPT
@IPCHAINS@ -A input -i @INTDEV@ -s @INTNET@ 1:1023 -d @INTIP@ 2049 -p udp -j ACCEPT
@IPCHAINS@ -A output -i @INTDEV@ -s @INTIP@ 2049 -d @INTNET@ 1:1023 ! -y -p udp -j ACCEPT
